<%= form_for(user_like) do |f| %>
  <% if user_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_like.errors.count, "error") %> prohibited this user_like from being saved:</h2>

      <ul>
      <% user_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :video_id %>
    <%= f.text_field :video_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
