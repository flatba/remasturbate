<%= form_for(video_like) do |f| %>
  <% if video_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_like.errors.count, "error") %> prohibited this video_like from being saved:</h2>

      <ul>
      <% video_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video_id %>
    <%= f.text_field :video_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
