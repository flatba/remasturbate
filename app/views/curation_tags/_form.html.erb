<%= form_for(curation_tag) do |f| %>
  <% if curation_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curation_tag.errors.count, "error") %> prohibited this curation_tag from being saved:</h2>

      <ul>
      <% curation_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="field">
    <%= f.label :curation_id %>
    <%= f.text_field :curation_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
